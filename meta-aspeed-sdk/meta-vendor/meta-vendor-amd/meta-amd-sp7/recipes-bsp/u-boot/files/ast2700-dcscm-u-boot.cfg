CONFIG_ASPEED_LTPI_GPIO=y
CONFIG_BOOTCOMMAND="ltpi -T 2000000 -p 1;echo Boot from spi; fdt addr ${fdtspiaddr} && fdt header get fitsize totalsize && cp.b ${fdtspiaddr} ${loadaddr} ${fitsize}; bootm 403000000#conf-aspeed-bmc-amd-kenya.dtb"
